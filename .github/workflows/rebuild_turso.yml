name: Rebuild Turso From All HF Snapshots
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired normalized publisher. Run Build and Publish Packed Turso in plan mode.' >> "$GITHUB_STEP_SUMMARY"
          echo 'serving.db is offline only; no database was changed.' >> "$GITHUB_STEP_SUMMARY"
